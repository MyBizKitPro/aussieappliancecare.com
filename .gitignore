# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log

# Local dev tooling (not needed on the live site)
.claude/

# Node deps (Netlify installs these at deploy time)
node_modules/

# Leftovers
images/_nettest.jpg

# Internal handoff notes (keep local, not on the public repo)
PROJECT-NOTES.md
